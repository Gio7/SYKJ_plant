import 'package:flutter/material.dart';

class ShopFromMain extends StatelessWidget {
  const ShopFromMain({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}